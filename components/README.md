# Components

Shared multi-element widgets used across multiple pages (nav bars, modals, repeated form groups).
Components are plain classes taking a `Page` (or a scoped `Locator`) in their constructor and are
composed into page objects — they don't extend `BasePage`.
