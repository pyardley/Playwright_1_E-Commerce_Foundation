services:
  perf-db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: perf
      POSTGRES_PASSWORD: perf
      POSTGRES_DB: perf
    ports:
      - "5432:5432"
    volumes:
      - perf-db-data:/var/lib/postgresql/data

volumes:
  perf-db-data:
